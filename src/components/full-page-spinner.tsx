const FullPageSpinner = (): React.ReactElement => (
  <div className="flex h-screen w-screen flex-col items-center justify-center">
    <img aria-label="Loading" src={""} alt="OL initial logo" />
  </div>
);

export { FullPageSpinner };
